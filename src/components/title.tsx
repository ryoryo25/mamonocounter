import { TITLE } from "@/libs/constants"

const Title = () => {
  return (
    <header className="mt-20 mb-8">
      <h1 className="text-3xl font-bold flex justify-center">{TITLE}</h1>
    </header>
  )
}

export default Title
